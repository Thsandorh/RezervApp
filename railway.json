{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd rezervapp && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd rezervapp && npx prisma db push --skip-generate && npx prisma db seed && npm start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
